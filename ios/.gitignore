# Generated by XcodeGen — regenerate with `xcodegen generate` (see README.md).
# The .xcodeproj (and the app target's Info.plist, written from project.yml's
# `info.properties`) are intentionally not committed; project.yml is the
# source of truth.
*.xcodeproj
FlightDeckRemote/Info.plist

# Xcode user state
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/

# Build artifacts
.derived*/
DerivedData/
build/
# scripts/archive.sh output: the .xcarchive, the exported .ipa and a signed
# ExportOptions.plist. Hundreds of megabytes, and the .ipa is a signed binary.
.build-archive/

# SwiftPM
.swiftpm/
.build/

# macOS
.DS_Store
